#include "../inc/push_swap.h"

void    ft_sort(ps_list **list)
{
    ps_list *n;
    ps_list *head;

    n = *list;
    head = *list;
    if (*list && list->next != 0)
    {
        while (list->next != 0)
        {

        }
    }
}